const express = require('express');
const router = express.Router();

app.post('/api/login', async(req, res) => {
	res.status(200);
	
	//Placeholder -- will replace with actual POST request
	//Need to know USER SCHEMA
	res.send('POST user email and password');
})


